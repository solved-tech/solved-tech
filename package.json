{
  "name": "solved-tech",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node scripts/prerender.mts",
    "preview": "vite preview",
    "test": "vitest run",
    "test:responsive": "playwright test",
    "check": "tsc",
    "check:csp": "node scripts/check-csp.mts"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}
